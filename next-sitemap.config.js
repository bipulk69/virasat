/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://thevirasat.com', // Update this with your actual domain
    generateRobotsTxt: true, // Generates robots.txt along with sitemap.xml
    sitemapSize: 5000, // Optional: Number of URLs per sitemap file
  };
  